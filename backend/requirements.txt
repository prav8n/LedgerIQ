# ---- Web framework ----
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# ---- Settings & validation (Pydantic v2) ----
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0

# ---- Database (async SQLAlchemy) ----
SQLAlchemy[asyncio]==2.0.36
alembic==1.14.0
asyncpg==0.30.0          # PostgreSQL async driver (primary)
aiosqlite==0.20.0        # SQLite async driver (local fallback)
greenlet==3.1.1          # required by SQLAlchemy async

# ---- Auth / security ----
python-jose[cryptography]==3.3.0
bcrypt==4.2.1

# ---- Background tasks ----
APScheduler==3.11.0

# ---- File import (CSV/XLSX) ----
openpyxl==3.1.5

# ---- HTTP / misc ----
httpx==0.28.1

# ---- Testing (dev) ----
pytest==8.3.4
pytest-asyncio==0.25.2
